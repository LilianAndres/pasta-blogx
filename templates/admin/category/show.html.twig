{% extends 'base.admin.html.twig' %}

{% block content %}
    <h1>Category</h1>

    <div class="container">
        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
            </tbody>
        </table>
    </div>


    <a href="{{ path('admin_category_index') }}" class="btn btn-primary">Retour à la liste</a>

    <a href="{{ path('admin_category_edit', {'id': category.id}) }}" class="btn btn-primary">Editer</a>

    {{ include('admin/category/_delete_form.html.twig') }}
{% endblock %}
